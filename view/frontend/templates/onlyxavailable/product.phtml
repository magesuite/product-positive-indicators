<?php
    $qty = $block->getProductQty();
    $displayFlag = $block->shouldDisplayInfoOnProductPage($qty);
?>
<?php if ($displayFlag) {
    if ($qty == 1) {
        /* @noEscape */ echo __('<span>Hurry up!</span> Only <span>1 product</span> available!');
    } else {
        /* @noEscape */ echo __('<span>Hurry up!</span> Only <span>%1 products</span> available!', $qty);
    }
} ?>
